# Philippine Property Price Prediction Neural Network Requirements

# Core Data Science Libraries
pandas>=1.5.0
numpy>=1.21.0
matplotlib>=3.5.0
seaborn>=0.11.0
scikit-learn>=1.1.0

# Deep Learning Framework
# Note: TensorFlow supports Python 3.9-3.12. For macOS Apple Silicon, use tensorflow-macos
# If using Python 3.14+, you'll need to use Python 3.12 or 3.11 instead
tensorflow-macos>=2.10.0; sys_platform == "darwin"
tensorflow>=2.10.0; sys_platform != "darwin"

# Geospatial Libraries
geopy>=2.2.0
folium>=0.12.0

# Additional Utilities
joblib>=1.1.0
plotly>=5.10.0

# Development and Analysis Tools
jupyter>=1.0.0
ipywidgets>=7.6.0

# System Requirements
# - Python 3.9-3.12 (TensorFlow does not support Python 3.13+ yet)
# - 8GB+ RAM recommended for neural network training
# - GPU optional but recommended for faster training
# - For macOS Apple Silicon: tensorflow-macos will be installed automatically

# Installation Instructions:
# 1. Create virtual environment: python -m venv property_prediction_env
# 2. Activate environment: 
#    - Windows: property_prediction_env\Scripts\activate
#    - macOS/Linux: source property_prediction_env/bin/activate
# 3. Install requirements: pip install -r requirements.txt
# 4. Run main script: python main.py

# Optional: For GPU acceleration (CUDA-enabled TensorFlow)
# tensorflow-gpu>=2.10.0 (only if you have NVIDIA GPU)